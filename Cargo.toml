[workspace]
resolver = "3"

members = [
    "limit-order-book",
]

[workspace.package]
edition = "2024"
rust-version = "1.90.0"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"

[profile.bench]
inherits = "release"
debug = true
