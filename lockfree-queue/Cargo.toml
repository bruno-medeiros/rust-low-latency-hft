[package]
name = "lockfree-queue"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Lock-free queues: SPSC (single-producer single-consumer) and MPSC (multi-producer single-consumer)"

[dependencies]
